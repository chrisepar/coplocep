import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    labelCore: {
        fontWeight: "bold"        
    },
    labelRoot: {
        backgroundColor: "white",
        fontWeight: "bold"
    },
    labelAsterisk: {
        color: "red"
    }
}));