import { makeStyles } from 'tss-react/mui';

export default makeStyles()((theme) => ({
    labelCore: {
        fontWeight: "bold"        
    },
    labelRoot: {
        backgroundColor: "white",
        fontWeight: "bold"
    },
    labelAsterisk: {
        color: "red"
    }
}));