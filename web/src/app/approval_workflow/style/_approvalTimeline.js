import { makeStyles } from 'tss-react/mui';

export default makeStyles()((theme) => ({
    tableHeadCell: {
        fontWeight: "Bold",
        width: 200
    },
    container: {
        maxHeight: 450
    }
}));